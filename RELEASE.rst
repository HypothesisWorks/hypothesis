RELEASE_TYPE: patch

This has no user-visible changes except one slight formatting change to one docstring, to fix an error that arises from changing our continuous integration build/test settings to treat deprecation warnings as errors.
