RELEASE_TYPE: patch

This patch fixes a bug where a lambda could be displayed with the definition of
an unrelated lambda which happened to take the same arguments - for example a
function generated by :func:`~hypothesis.strategies.functions` when shown in a
failing example.
