RELEASE_TYPE: patch

This release lays internal groundwork for re-encoding values produced by
strategies which cannot currently be inverted, such as ``.map(...)`` and
|st.composite|. There is no user-visible change yet.
