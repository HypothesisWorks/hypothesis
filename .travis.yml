language: c

sudo: false

env: PYTHONDONTWRITEBYTECODE=x

os:
    - linux

branches:
  only:
    - "master"

cache:
    cargo: true
    apt: true
    directories:
        - $HOME/.rustup
        - $HOME/.runtimes
        - $HOME/.venv
        - $HOME/.cache/pip
        - $HOME/wheelhouse
        - $HOME/.stack
        - $HOME/.local
        - hypothesis-ruby/vendor/bundle

env:
    global:
        - BUILD_RUNTIMES=$HOME/.runtimes
        - FORMAT_ALL=true


# Temporary hack to try and get this working
install:
  - rm -rf ~/.rvm


jobs:
  include:
    # Prechecks that we want to run first.
    - stage: precheck
      env: TASK=lint-ruby

script:
    - ./build.sh

matrix:
    fast_finish: true

notifications:
  email:
    recipients:
      - david@drmaciver.com
    on_success: never
    on_failure: change

addons:
  apt:
    packages:
      - libgmp-dev
