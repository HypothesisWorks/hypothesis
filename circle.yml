test:
  override:
    - scripts/run_circle.py

machine:
  pre:
    - brew install python

dependencies:
  override:
    - make install-core

  cache_directories:
    - ~/.cache/hypothesis-build-runtimes
