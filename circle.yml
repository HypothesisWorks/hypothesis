test:
  override:
    - scripts/run_circle.py

machine:
  pre:
    - brew install python
    - python --version
    - pip --version

dependencies:
  override:
    - make install-core

  cache_directories:
    - ~/.cache/hypothesis-build-runtimes
