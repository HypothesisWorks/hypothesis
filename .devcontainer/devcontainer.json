{ 
  "name": "HypothesisWorks/hypothesis",
  "image": "python:3.8-slim-buster",

  "settings": {
    "python.pythonPath": "${workspaceFolder}/.venv/bin/python3",
  },

  // After conatainer is created, we install all the requirments,
  // and show what commands there are in our own tooling:
  "postCreateCommand": "apt-get update && apt-get install -y shellcheck && make check-installed",
}
